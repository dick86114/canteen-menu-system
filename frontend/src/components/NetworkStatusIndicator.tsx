import React from 'react';

const NetworkStatusIndicator: React.FC = () => {
  // 暂时禁用网络状态检查，避免初始化时的错误提示
  // const { isOnline, isConnected, checkConnection } = useNetworkStatus();
  
  // 网络状态指示器组件 - 暂时禁用，避免误报
  // 在实际部署时可以重新启用
  
  return null;
};

export default NetworkStatusIndicator;